import { TemplateOptions } from '../../TemplateOptions';
import { DestructuringInfo } from '../../../extractors/destructuring/DestructuringInfo';
export class DestructuringSummaryMaker {
    public make(destructuring: DestructuringInfo[], options: TemplateOptions): string {
        const lines: string[] = [];
        destructuring.forEach(destructuring => {});
        return '';
    }
}
