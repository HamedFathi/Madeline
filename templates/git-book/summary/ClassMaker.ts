import { ClassInfo } from './../../../extractors/class/ClassInfo';
import { TemplateOptions } from '../../TemplateOptions';
export class ClassMaker {
    public make(classes: ClassInfo[], options: TemplateOptions): string {
        let lines: string[] = [];
        classes.forEach(cls => {

        });
        return '';
    }
}


